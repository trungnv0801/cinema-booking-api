<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml" />

  <springProfile name="prod">
    <property name="LOG_FILE" value="${LOG_FILE:-logs/app.log}" />
    <include resource="org/springframework/boot/logging/logback/file-appender.xml" />

    <property name="REQUEST_LOG_FILE" value="${REQUEST_LOG_FILE:-logs/requests/request.log}" />
    <property name="REQUEST_LOG_MAX_HISTORY" value="${REQUEST_LOG_MAX_HISTORY:-30}" />
    <appender name="REQUEST_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
      <file>${REQUEST_LOG_FILE}</file>
      <encoder>
        <pattern>${FILE_LOG_PATTERN}</pattern>
      </encoder>
      <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
        <fileNamePattern>${REQUEST_LOG_FILE}.%d{yyyy-MM-dd}.gz</fileNamePattern>
        <maxHistory>${REQUEST_LOG_MAX_HISTORY}</maxHistory>
      </rollingPolicy>
    </appender>

    <logger
        name="com.example.cinema_booking.shared.filters.RequestLoggingFilter"
        level="INFO"
        additivity="false">
      <appender-ref ref="REQUEST_FILE" />
    </logger>

    <root level="INFO">
      <appender-ref ref="FILE" />
    </root>
  </springProfile>

  <springProfile name="!prod">
    <include resource="org/springframework/boot/logging/logback/console-appender.xml" />
    <root level="INFO">
      <appender-ref ref="CONSOLE" />
    </root>
  </springProfile>
</configuration>
